define([
  'glimpse'
],
function (glimpse) {
  'use strict';

  describe('glimpse', function () {
    it('should have a version', function () {
      expect(glimpse.version).to.exist;
    });
  });

});
